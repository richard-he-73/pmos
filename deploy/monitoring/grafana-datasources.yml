apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

  - name: MongoDB
    type: grafana-mongodb-datasource
    access: proxy
    url: http://mongodb:27017
